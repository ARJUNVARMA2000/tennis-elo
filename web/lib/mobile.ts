import type { Viewport } from "next";

/** Browser chrome and viewport behavior for the app's always-dark shell. */
export const APP_VIEWPORT: Viewport = {
  colorScheme: "dark",
  themeColor: "#08090a",
  viewportFit: "cover",
};
