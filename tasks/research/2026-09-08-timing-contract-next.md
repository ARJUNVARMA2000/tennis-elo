# Next session — collect qualifying timing evidence, not another document survey

Read the [review](2026-09-08-timing-contract-review.md),
[calibration/implementation protocol](2026-09-08-timing-calibration-protocol.md),
[result manifest](2026-09-08-timing-contract-result.json), live todo tail and
[WTA lifecycle handoff](2026-09-08-wta-lifecycle-next.md). The bounded document review
is complete. No new field qualification, model score or software implementation resulted.

## Exact resume point

`R = /Users/varma/Projects/DEUCE/.research/2026-09-06-model-foundation`.
Latest research checkout `R/worktrees/timing-contract`, branch
`codex/model-timing-contract`, based on `27cc95af1cb72aa56b316a6fcea30771a3d18094`.
Resolve its final documentation acceptance tip before creating the next isolated checkout.
Original DEUCE contains documents/logs only. Preserve accepted worktrees.

Program source remains the accepted WTA event auditor `f355253`, order/calendar adapters
`b6df856`, identity audit `aab6ed0`, US Open collector `cda5393`, and bounds evaluator
`10c07ee`. Only `synthetic-start-v1` is qualified. No live registration or recurring job
exists. Keep the corrected incumbent and deferred candidate unchanged.

## New evidence and preservation contract

`R/runs/timing-contract` contains four captures under `officials-2026`, `wta-2026`,
`wta-data-partnership`, `stats-perform-ma21`. Each has attempt/receipt/raw bytes.
PDF folders also contain byte-identical `source.pdf`, text extraction and the nine
visually inspected page renderings. `document-review.json` records page mappings and
bounded searches; `offline-replay.json` re-verifies the two prior event audits.

`protected-run-files.json` protects **635** earlier run files. The result manifest lists
**29** new files and hashes: **664** next-phase protected files. It also records 91 exact
frozen package files, both model hashes, 384 exact inherited program/web/workflow files,
33 tracked data files and fourteen clean prior checkouts. After this round is committed,
there are fifteen accepted research checkouts to preserve. Large external inventories
were last fully checked at **2026-09-08T01:58:44.035725+00:00**.

Private drivers `R/tools/timing-contract-probe.py` and `timing-contract-verify.py` are
hashed in the manifest. Both write exclusively to this accepted run; do not rerun them
over existing destinations. The fourth allowlist slot changed from an unused Rules of
Tennis URL to the observed primary MA21 documentation URL before acquisition. Exactly
four retained reads succeeded; no authenticated service endpoint was called.

## Next action decision

1. **Prefer one concrete independent witness.** Use protocol A to determine whether an
   accessible first-serve observation has a defensible UTC/latency bound and exact match
   identity. Do not assume ordinary broadcast timestamps, local file metadata, another
   scoreboard or shared elapsed time meet this. Missing clock provenance is a failed
   feasibility check, not a reason to invent a tolerance. Do not buy access or contact
   providers implicitly. No user credentials are needed for the existing public adapters.
2. **Observe genuinely new official source versions when available.** Last US Open WTA
   order observation was **September 8 at 13:32:46.632425 UTC**, unchanged from 05:41 UTC;
   its first listed court start was 15:30 UTC. Check actual current time. Do not repeat
   pre-play reads immediately. This round intentionally did not fetch match feeds again.
   Guadalajara 2075/2026 was last unpublished at 05:35 UTC September 8, with play scheduled
   September 13–19 in retained calendar evidence. Reconfirm publication before using any
   2026 match IDs; never carry over 2025 `LS001`/`LS002` as current fixtures.
3. **Implement only the behavior newly supported by 1/2.** Protocol C/D names the proposed
   modules, immutable fields and failure cases. Real version comparison can proceed
   independently of clock qualification. A live start adapter cannot. If neither new
   physical evidence nor source progression is available, report the dependency and end
   the bounded session; do not manufacture another audit layer or restart generic shopping.
4. **Registration follows qualification and coverage.** Preserve the strict primary gate,
   future registration, 30-day/day-37 windows and 200-pair adequacy check. Neither elapsed
   waiting nor a promising reported marker backdates predictions or qualifies old samples.

Current primary manuals support first-serve semantics but do not bridge the public feed
to an independently bounded physical clock. MA21 is a distinct authenticated interface;
its `actualStartDate` definition cannot be transferred to public WTA `Timestamp` by name.
The reviewed 2026 rules do not prove compliance in the 2025 sample. No fresh general
documentation search is warranted without a specific new primary lead.

## Verification and closure

Use `uv`, the existing coordinator interpreter and `PYTHONPATH=src:research` from the
owning worktree's `tennis_model` directory. The previous WTA lifecycle handoff provides
the exact focused test and read-only CLI recipes. There were **no new suite runs** in
this documentation round; 296 passes/11.34s belongs to the preceding code change.

Reconcile Git before new repository claims. Verify all 664 protected files, package/model
hashes and accepted checkouts before/after work. Do not copy training data or refit to
resolve source timing. Append a bounded plan and completion review to the logs, record
actual checks, commit research and mirror only documents/logs to original DEUCE.
No agents, account, provider contact, automation, merge/push/deploy is implicit.
