# Resume here — qualify a published lower bound before further timing code

Read the [source review](2026-09-07-start-source-review.md),
[receipts and diagnostics](2026-09-07-start-source-result.json), the current todo tail,
and the [accepted evidence interface](2026-09-07-time-evidence-interface.md).
Reconcile Git before writing a new plan. The user has no provider subscription and
asked about cost; they have not authorized signup, a trial, purchase or provider contact.
The bounded public timeline investigation is finished. Do not repeat its 18 reads.

## Exact retained state

`R = /Users/varma/Projects/DEUCE/.research/2026-09-06-model-foundation`.
Latest source findings: `R/worktrees/start-source`, branch `codex/model-start-source`,
based on `129f811`. Implementation remains `10c07ee`; neither the package nor evaluator
changed. Raw responses and offline census are in `R/runs/start-source`; private drivers
are in `R/tools`. The result JSON pins their hashes, each receipt and 443 older run files.
Include this phase's `runFiles` manifest in the next preservation inventory.

Frozen incumbent: `R/runs/maintenance/wta-final-001/predictor.pkl` plus `.envelope`;
SHA-256 `8cf280c43f813a6944244d585248af8d5fcbc1ff628e535f98d4e10866730e34`.
Candidate: `R/runs/prospective-shadow/migration-001/candidate.shadow`;
SHA-256 `43dd88b1461913f11b90b6d2aa6aa82f7bbfe3ba7ad597d978c6a46b73d45fcc`.
Provenance and implementation freeze stay in `R/runs/prospective-shadow`.
No live pilot or collection cadence exists. Synthetic QA is never live evidence.

## T0 — one concrete free order-of-play qualification

This is the next untested route, not a promise that official schedules suffice.
Append its bounded plan to `tasks/todo.md` before edits. Use new exclusive acquisition
directories; no training-data copy, model refit or general evaluator is needed.

1. Reuse the retained `uso-config/response.bin`. Its `scoringData.scheduleDays` points
   to `/en_US/scores/feeds/2026/schedule/scheduleDays.json`; `schedule` uses
   `/en_US/scores/feeds/2026/schedule/schedule<day>.json`. Discover one actual day from
   the returned list, then inspect that day's row and linked official publication.
   The config also exposes a `schedulePdf` prefix; inspect the concrete publisher link
   before fetching a PDF, rather than inventing its suffix. Limit the first pass to
   six new HTTP reads, no retries or credentialed access.
2. Select an explicitly published **not-before** match, identify edition, court, round
   and real players, and retain publication/acquisition evidence and the original time
   with its timezone. An estimated slot, “followed by”, scoreboard timestamp or
   undated current page does not supply the required lower bound. The earlier Grand
   Slam rulebook interpretation is retained in the time-evidence interface; verify its
   applicability to this exact publication. Do not reread generic rules as a substitute.
3. Write down what is observed and what rests on tournament schedule compliance. A
   rule plus a posted time may support an explicitly assumption-based lower bound;
   it must not be renamed an observed start. Determine whether that evidence can meet
   the primary claim honestly. A weaker exploratory evaluation requires an explicit
   separate protocol and must not silently replace the independent confirmation gate.
4. Resolve earlier revisions, postponement, court moves and participant replacement
   before use. Preserve all conflicting versions; a late revision cannot change the
   original capture deadline. Pair any supported lower bound with independent start
   upper evidence to form a valid interval. A corroborated completed observation can
   be a coarse upper bound on start because start precedes finish; it may leave week
   grouping unavailable. It supplies no start lower bound by itself.
5. Finish with a field-level mapping, supported assumptions and a real-source verdict.
   If the six-read sample cannot support it, record the exact missing fact and stop
   that source path. Do not add another generic framework to represent absent evidence.

## T1 — implementation only after T0 succeeds

Create a new isolated checkout from the accepted source-review tip. Extend the external
evidence path (`tennis_model/research/time_evidence.py` / `prospective_bounds.py`) only
after a justified source contract exists; preserve all frozen model bytes. Inspect the
current evidence types first. If a new assumption-based type is needed, version it
explicitly and keep existing registrations immutable. Keep `check_mode()` refusing live
use until that producer and its acquisition behavior qualify.

The producer needs exact edition/match identity and raw-to-claim links; explicit bounds
and timestamp precision; stale/missing/contradictory response handling; and preserved
source revisions. Bind its version, semantics, coverage and qualification record in a
new registration. Known US Open contradictions must be exclusions, not day-offset repairs.
WTA/US Open agreement must not be assumed independent merely because hostnames differ.

Tests should exercise the chosen source's actual failure cases: earlier schedules,
wrong edition, replacement, untimed/preface events, missing first serve, mixed epoch
units, start-after-end and regressing starts, duplicate/revised events, 200 HTML, empty
events and 404. Use the real retained failures as provenance where the source applies.
Then run focused tests and full-size frozen-artifact integration with explicitly
synthetic clocks. Only new implementation justifies rerunning implementation tests.

## Optional access path

Sportradar is an alternative if free qualification fails; it is not a prerequisite
for the next free investigation. The review records official trial and timeline docs.
Before any signup, resolve permission for that account action. Before any purchase,
obtain a concrete quote and permission for its actual terms; neither is authorized now.

Access must cover the target WTA events, required `match_started`/`first_serve` records,
revisions, retention and the full capture/settlement interval. Verify how an event time
relates to physical first serve and its error bound. Request limits need a written
budget for catalogue, capture, timing and final reconciliation; do not assume frequent
whole-tour polling fits a trial. Do not ask the user to paste a key into the conversation.
Provider communication requires explicit authorization before sending.

## T2 — real activation, after source and cadence qualification

T0 precedes T1. Source qualification and access planning can proceed independently;
code and real lifecycle observations can overlap only once the source/observation plan
is defined. This describes task dependencies, not authorization for parallel agents.

A new actual registration, tested producer and real acquisition cadence are all required
before a live experiment. No background collector has been authorized. Follow the app's
automation flow if scheduling is requested. Preserve the fixed 30-day capture / day-37
settlement endpoint, unless a separately justified protocol change is registered before
outcomes. Keep `capture + 5 minutes < original schedule lower <= supported start lower`.

Report coverage, missing/conflicting timing and paired scores at the fixed endpoint.
The 200-pair threshold is an operational coverage check, not demonstrated statistical
power for the small historical improvement. More time supplies new independent outcomes;
it does not repair an invalid timing source. Candidate adoption still needs the arbiter
gate and uncertainty assessment. Do not buy data simply to declare this phase complete.

Close out with Git reconciliation, raw/run/model preservation, checked append-only todo
review and ledger entry. Mirror only documents and logs into the original checkout;
no production merge/push is implicit.
