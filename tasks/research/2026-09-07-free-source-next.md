# Next implementation — free official schedule collection

Read the [free-source review](2026-09-07-free-source-review.md),
[result manifest](2026-09-07-free-source-result.json), live todo tail and accepted
`2026-09-07-time-evidence-interface.md`. The public order investigation is complete.
Do not repeat the six reads, the prior 18 point-feed reads or generic source searches.

## Fixed state and reusable work

`R = /Users/varma/Projects/DEUCE/.research/2026-09-06-model-foundation`.
Source checkout: `R/worktrees/free-source`, branch `codex/model-free-source`, from
`2e50cb5`. Package/evaluator implementation still comes from `10c07ee`; no new package,
evaluator or test code was written this phase. Runs: `R/runs/free-source`.

Private `R/tools/free-source-probe.py` handles bounded single public reads with exclusive
raw receipts. `R/tools/free-source-analyze.py` checks both daily orders and extracts
six WTA singles rows into `schedule-observations.json`. Use it as the field-mapping
reference, not as an integrated producer. It hardcodes this audited edition/sample,
has no revision store and must not accept arbitrary live evidence.

Use the preserved coordinator runtime through `uv run --offline --no-project --python
R/worktrees/coordinator/tennis_model/.venv/bin/python`. From `tennis_model`, set
`PYTHONPATH=src:research` and use paths beginning `research/` or `tests/`. Keep driver
paths absolute; resolve test filenames before invoking them.

The frozen incumbent is `R/runs/maintenance/wta-final-001/predictor.pkl` (+ envelope),
SHA `8cf280c43f813a6944244d585248af8d5fcbc1ff628e535f98d4e10866730e34`.
Candidate: `R/runs/prospective-shadow/migration-001/candidate.shadow`,
SHA `43dd88b1461913f11b90b6d2aa6aa82f7bbfe3ba7ad597d978c6a46b73d45fcc`.
Provenance/freeze stay under `R/runs/prospective-shadow`; all 500 prior run files were
verified. Add this phase's `runFiles` manifest to the next protected inventory.

## U0 — define the source-specific collection contract

Append the next bounded plan before edits. Work in a new isolated checkout from the
accepted free-source tip, without training copies. The original checkout continues
to receive only research documents and append-only logs.

Collection of **published schedule observations** is now justified. Qualification as
**actual-start evidence** is a separate unresolved prerequisite for live evaluation.
Do not change `time_evidence.check_mode()` or relabel the extracted observations.
Do not implement another general evaluator to hide that distinction.

Freeze these concrete choices before implementation:

1. Explicit edition and allowed official hosts. Resolve released day URLs from the
   catalogue and validate their year/day against the document and court clocks. Do
   not infer main-draw day from `tournDay`; do not infer match date from catalogue epoch.
2. Source key `(edition, match_id)`, actual WTA singles event code, two real WTA IDs,
   round, court/session and sequence. Use established identity evidence before joining
   to ESPN or model context; event-name similarity and six isolated witnesses are insufficient.
3. Keep `explicit-not-before`, `first-match-session-start`, and sequence-only rows
   distinct. Convert printed local clocks with the declared venue zone. Preserve the
   original strings, bounds assumptions, response time and raw digest. Empty comments
   or conjunctions must not manufacture a start time for a later match.
4. Save immutable versions. Earlier time, new opponent, changed court/session, cancelled
   or missing match and conflicting edition remain visible; a fresh response never
   overwrites the first captured forecast/context. A transport error is not an empty order.
5. Treat source publication hints as hints. Local receipt time proves when we acquired
   bytes; time-of-day `releaseTime` and generic `epoch` do not prove first publication.
   A PDF generated by the same publisher checks layout/meaning, not independent play.

## U1 — implement and test that collector

Build one external source-specific module and reuse existing receipt/intake helpers
where compatible. Start with offline retained-fixture extraction and version comparison;
add bounded explicit fetches after those checks. It should emit schedule observations,
coverage/exclusion reasons and source links, without inventing actual-start events.

Required cases come from the real sample: day-17 catalogue epoch belongs to the prior
calendar date; mixed juniors/doubles/wheelchair rows; intentional blank; encoded PDF
text; two sessions on the same court; first-session versus explicit not-before. Also
test malformed/duplicate IDs, participant replacement, earlier revisions, timezone
mismatch, stale/missing payloads, 200 HTML, incomplete bodies and repeated captures.
Keep each case tied to an actual contract risk rather than mirroring helper internals.

Run focused tests for new code and an offline replay of both retained days. Check exact
raw/model preservation. No model fitting, migration, new dependency or full-suite rerun
is needed merely to add source-only observation collection; broaden checks only when
new integration risk warrants it. Do not turn these retrospective fixtures into forecasts.

## U2 — qualify timing and real collection separately

U0 precedes U1. A bounded real revision/lifecycle sample can overlap implementation once
its acquisition plan is defined; it cannot be fabricated from the existing two orders.
This describes parallel work dependencies, not authorization to spawn agents.

The audited day-16 not-before match was already past its published time when fetched.
The two future day-17 singles observations use session-start labels. Neither sample
validates a complete live start lifecycle. Collect a genuinely future explicit not-before
case and its later source versions before claiming such qualification. A source read
after a hypothetical earlier capture cannot backdate forecast evidence.

Before admitting a schedule-based lower bound, justify the applicable publication/rule
contract and state tournament-compliance assumptions explicitly. If those assumptions
cannot meet the primary independent-confirmation claim, keep the data as operational
schedule evidence and leave the live arbiter blocked on its actual missing premise.
Any exploratory assumption-based evaluation must be clearly preregistered and separate
in its reporting; it cannot silently weaken or replace the accepted primary gate.

A live pilot still requires a tested qualified producer, new actual registration and a
real acquisition cadence. No unattended collector is active. Use app automation tools
only when scheduling is requested. Keep the existing fixed 30-day capture / day-37
settlement endpoint and the pre-play inequality from the evidence interface unless a
justified protocol amendment is recorded before outcomes. Two early schedule receipts
are not forecasts; 200 pairs remains a coverage threshold, not a power calculation.

## Alternative-provider decision and closeout

The review records current free-tier documentation. No free API account was created.
A new scoreboard account would not by itself solve the required start-event evidence;
prioritize the demonstrated official route over signup for its own sake. No paid access,
trial, provider contact or account action is authorized by this handoff.

Preserve all completed checkouts, raw observations, source manifests, model artifacts
and old QA. Reconcile Git, append review/ledger/lessons, commit research and mirror only
documents/logs. No production merge, push or deploy is implicit.
