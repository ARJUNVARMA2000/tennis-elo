# Dependencies for the WEEKLY alias proposer only (.github/workflows/propose-aliases.yml).
#
# Deliberately separate from requirements.txt. Those pins come from the last successful CI
# install log and exist to keep the daily retrain deterministic and its pickles loadable
# across runs (AGENTS.md); nothing in this file is imported by the pipeline, the gate, or
# the site build, so a bump here cannot move a single model probability.
#
# The OpenRouter transport uses Python's standard-library HTTP client. This file remains as
# the workflow's explicit dependency boundary: adding an LLM client here is a deliberate,
# reviewable change, while tests/test_alias_proposer.py pin the HTTP contract without touching
# the network.
