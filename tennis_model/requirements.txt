# Exact pins: the daily CI retrain must be deterministic — an unnoticed
# pandas/xgboost/sklearn bump can silently shift model probabilities mid-stream.
# Pinned to the versions the production artifacts (cached predictor.pkl, feature
# pickles) were built with in CI — NOT a local dev env; unpickling across sklearn
# versions breaks (learned the hard way: 1.9.0 pickle + 1.5.2 runtime = crash).
# Bump deliberately: update the pin, run pytest AND one full pipeline before merging.
pandas==3.0.3
numpy==2.5.0
scikit-learn==1.9.0
xgboost==3.3.0
openpyxl==3.1.5   # only for reading Tennis-Data odds spreadsheets (eval/compare)
xlrd==2.0.2       # Tennis-Data serves legacy .xls content under .xlsx names
pytest==8.3.3     # kept here so CI and local runs agree
# optuna          # OPTIONAL, local only: hyperparameter sweeps (eval/tune) — not needed by CI
