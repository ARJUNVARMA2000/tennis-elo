pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
openpyxl>=3.1   # only for reading Tennis-Data odds spreadsheets (eval/compare)
# optuna        # OPTIONAL, local only: hyperparameter sweeps (eval/tune) — not needed by CI
